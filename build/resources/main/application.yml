spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/jwt_springsecurity?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: ghddydwns0928!!

  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    generate-ddl: true
    show-sql: true
  redis:
    port: 6379
    host: localhost
logging:
  level:
    org.hibernate.sql: debug # hibernate가 생성하는 모든 sql이 log로 보인다.
    #    org.hibernate.type: trace  # p6spy를 사용하면 굳이 사용할 필요가 없다.
server:
  address:
  compression:
    enabled: true
  port: ${port:8080}
jwt:
  token:
    secret: "missyouannawhereareyou"
